[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "the-central-command"
version = "0.1.0"
description = "DKI Engine - Central Command System"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
namespaces = false

